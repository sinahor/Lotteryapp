import React from 'react';
import ReactDOM from 'react-dom/client';
// import Navigation from './Navigation';
import Navigation from './components/Unitsold';





const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Navigation/>
  </React.StrictMode>
);
